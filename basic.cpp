#include<bits/stdc++.h>
using namespace std;
typedef vector<int> vi;
//#define rep(i,a,b) for(int i=a;i<=b;i++)
int main(){
    vi v;
    v.push_back(5);
    v.push_back(3);
    v.push_back(1);

    for(int i=0;i<v.size();i++)
        printf("%d ",v[i]);
    // for(auto x : v)
    //     printf("%d ",x);
}  